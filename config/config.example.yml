aws:
  credentials:
    access_key_id: "access_key_id"
    secret_access_key: "secret_access_key"
processors:
  sample:
    queue: 'sample' # can be omitted
    publishers:
      delay: 30
    workers:
      initial: 3